# Add the executable
add_executable(main main.cpp)

# Include directories
target_include_directories(main PRIVATE 
    ${CMAKE_SOURCE_DIR}/include
    ${CMAKE_SOURCE_DIR}/lib
)

# Link libraries
target_link_libraries(main PRIVATE brilcpp)
